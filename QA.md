# PWA 版本 QA

版本：1.8.1
执行日期：2026-09-23

| 检查项 | 结果 |
| --- | --- |
| HTML 主线程 / Worker / Service Worker 语法 | 通过 |
| 增量 SHA-256 | 通过：标准向量 `abc` 得到 `ba7816bf...15ad`，不再整文件调用 `arrayBuffer()` |
| 流式 CSV | 通过：跨分块状态解析，合成报告覆盖引号、逗号及字段内换行 |
| 二进制磁盘分页 | 通过：`.aba2` 按100条建立随机访问偏移，页面只返回当前页最多200条 |
| 美国站单文件压力测试 | 通过：1,646,370,074 字节、2,785,146 条，约64秒完成导入及查询验收；未出现原版内存错误 |
| 美国站双文件压力测试 | 通过：合计约2.85 GiB、5,545,986 条，两份同站点报告约116秒完成导入并自动进入真实周次对比 |
| 美国站本地占用 | 通过：第29周分析数据125,705,455字节、趋势33,421,764字节；第33周分析124,564,209字节、趋势33,130,092字节 |
| 美国站随机页码 | 通过：2,760,840条数据直接跳转第10,000页并只返回200条 |
| 美国站全库筛选 | 通过：`water bottle` 在第33周筛得7,591条，并完成真实双周涨跌排序 |
| 旧版数据兼容 | 通过：缺少 `storageVersion` 的1.7.0 gzip报告由新Worker正常加载，不要求重新导入 |
| 浏览器级导入 | 通过：Edge Headless 实际运行 Web Worker、IndexedDB、OPFS 和 gzip 流 |
| 导入前重复检测 | 通过：首次导入2份后再次选择相同2份，Worker解析次数未增加、目录仍为2份并提示跳过2份 |
| 部分重复导入 | 通过：同时选择1份已导入及1份新报告，只新增解析1份并提示“导入1份，跳过1份” |
| 同批次重复检测 | 通过：同次选择两份同内容、不同文件名报告，只解析并保存1份 |
| 旧版重复检测兼容 | 通过：移除元数据快速指纹后，以站点、原文件名及文件大小命中并跳过，不启动Worker |
| 重复检测元数据 | 通过：新报告保存64位十六进制 `sourceFingerprint` 与 `sourceLastModified` |
| 超大文件快速指纹 | 通过：大于2 MiB的文件仅读取开头1 MiB与结尾1 MiB，不调用整文件 `arrayBuffer()` |
| 多站点批量导入 | 通过：一次导入 GB 31/32 周及 DE 33 周，目录生成 3 份报告 |
| 统一导入入口 | 通过：单份、两份同站点自动对比、多份/混合站点批量导入使用同一个多选文件入口 |
| 本地数据查看 | 通过：可列出源报告原始大小参考、压缩分析文件、趋势索引文件及各自大小 |
| 本地数据站点筛选 | 通过：隔离浏览器中导入 GB 两周、DE 一周、FR 一周；筛选 GB 后仅显示 2 份，三类空间统计同步变为 GB 汇总值 |
| 多选和全选 | 通过：全选只勾选当前 GB 筛选结果，删除按钮显示“删除选中（2）” |
| 批量删除 | 通过：GB 两份报告的 IndexedDB 元数据、OPFS 分析文件、趋势索引全部删除；DE/FR 保留，趋势内存缓存清零 |
| 源报告安全边界 | 通过：只记录导入时文件大小并标记“未复制”；删除本地数据不删除电脑原位置的 CSV |
| 普通文件夹导出 | 已配置 Chromium File System Access API，导出数据文件、`reports.json` 和说明文件 |
| 默认最新报告 | 通过：批量导入后与重载后均自动显示 DE 33 周 |
| 刷新持久化 | 通过：重载页面后不再选择文件，自动恢复 3 份报告及最新数据 |
| 站点切换 | 通过：DE 切换至 GB 后自动加载 GB 最新周 |
| 同站点双周对比 | 通过：GB 32 周对比 31 周 |
| 涨跌计算 | 通过：`beta` 新排名 1、旧排名 2、涨跌 +1；`gamma` 旧周不存在时为 0、涨跌 -2 |
| TOP3 聚合 | 通过：合成数据 `beta` 点击 10% + 10% + 10% = 30% |
| 三周排名趋势 | 通过：`alpha` 为 `[1, 空, 3]`，`beta` 为 `[2, 1, 5]`，周次顺序为 31→32→33 |
| 缺失周处理 | 通过：中间周未查询到 `alpha` 时保存 `null`，折线断开而非排名 0 |
| 趋势持久化 | 通过：刷新后从 OPFS 二进制索引恢复相同三周序列 |
| 旧报告自动迁移 | 通过：删除 32 周趋势索引及元数据后，后台自动重建并写回 `trendVersion: 1` |
| 真实 GB 周报解析回归 | 通过：427,392 条；`water bottle` TOP3 点击 20.38%、TOP3 转化 6.74% |
| 真实 GB 趋势索引 | 通过：427,392 条生成 5,128,716 字节二进制索引，当前页生成 200 个 Canvas 趋势图 |
| CSV 首行周次 | 通过：识别 `周 36 \| 2026-08-30 - 2026-09-05`，去除末尾重复年份 |
| 双栏分页与筛选 | 保留 1.3.1 回归结果：每页最多 200 条，页码记忆、七项筛选、多列三态排序通过 |
| 十站点跳转映射 | 已配置 US、CA、MX、GB、DE、FR、IT、ES、JP、AU |
| PWA 外壳 | 已配置 manifest、图标、网络优先/缓存回退 Service Worker |
| 原始数据上传 | 无：仅读取用户选择的本地 CSV，并写入同源浏览器存储 |

边界：本版是“线上托管的纯前端 + 当前浏览器本地持久化”，不是云端数据库。清除站点数据、使用无痕模式或更换浏览器/电脑时，已导入数据不会自动跟随。Amazon 和 Google Trends 跳转仍需要网络。
